% This is samplepaper.tex, a sample chapter demonstrating the
% LLNCS macro package for Springer Computer Science proceedings;
% Version 2.21 of 2022/01/12
%
\documentclass[runningheads]{llncs}
%
\usepackage[T1]{fontenc}
% T1 fonts will be used to generate the final print and online PDFs,
% so please use T1 fonts in your manuscript whenever possible.
% Other font encondings may result in incorrect characters.
%
\usepackage{graphicx}
% Used for displaying a sample figure. If possible, figure files should
% be included in EPS format.
%
% If you use the hyperref package, please uncomment the following two lines
% to display URLs in blue roman font according to Springer's eBook style:
%\usepackage{color}
%\renewcommand\UrlFont{\color{blue}\rmfamily}
%\urlstyle{rm}
%

\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{float}
\begin{document}
%
\title{Emotional Models: Supervised vs. Zero-Shot Classification}
%
%\titlerunning{Abbreviated paper title}
% If the paper title is too long for the running head, you can set
% an abbreviated paper title here
%
\author{Miguel Castela\inst{1} \and
Miguel Martins\inst{2}} 
%
\authorrunning{M. Castela and M. Martins}
% First names are abbreviated in the running head.
% If there are more than two authors, 'et al.' is used.
%
\institute{Univsersity of Coimbra, Portugal \\
\email{uc2022212972@student.uc.pt}\\
\email{uc2022213951@student.uc.pt}
}
%
\maketitle              % typeset the header of the contribution
%
\section{Introduction}

This project presents the implementation and evaluation of two distinct approaches to emotion classification using Transformer-based models applied to the \textit{Emotion} dataset.
\noindent
\\
The main objective of this project is to compare the performance of two different methodologies for emotion classification. The first approach follows a supervised learning paradigm, where pretrained Transformer models are used as frozen feature extractors to generate sentence embeddings via models from the \texttt{sentence-transformers} library. The second approach adopts a zero-shot learning strategy based on Natural Language Inference (NLI) models, where emotion labels are expressed as natural language hypotheses and classification is performed without task-specific fine-tuning. By evaluating both supervised and zero-shot methods on different pretrained models from the HuggingFace Hub using the same dataset, this project aims to analyze their respective strengths and limitations, as well as characteristic error patterns.

\section{Description of the problem}

The \texttt{dair-ai/emotion} dataset consists of text samples, primarily collected from Twitter, labeled with one of six emotions: \textit{joy, sadness, anger, fear, love} and \textit{surprise}. The samples are distributed according to the following table:

\begin{table}[H]
\centering
\caption{Class distribution of the \texttt{dair-ai/emotion} dataset.}
\label{tab:dataset_dist}
\begin{tabular}{lcccc}
\hline
\textbf{Emotion} & \textbf{Train} & \textbf{Validation} & \textbf{Test} & \textbf{Total (\%)} \\
\hline
Joy      & 5,362 & 704 & 695 & 33.8\% \\
Sadness  & 4,666 & 550 & 581 & 29.0\% \\
Anger    & 2,159 & 275 & 275 & 13.5\% \\
Fear     & 1,937 & 212 & 224 & 11.9\% \\
Love     & 1,304 & 178 & 159 & 8.2\% \\
Surprise & 572   & 81  & 66  & 3.6\% \\
\hline
\textbf{Total} & \textbf{16,000} & \textbf{2,000} & \textbf{2,000} & \textbf{100.0\%} \\
\hline
\end{tabular}
\end{table}

\vspace{-2em}
\noindent
\\
By looking at table~\ref{tab:dataset_dist}, it's clear that the dataset is imbalanced with the \textit{joy} and \textit{sadness} classes being significantly more represented. Together, they account for over 60\% of the data. In contrast, the \textit{surprise} class is the least represented by a considerable margin, 3.6\%. This imbalance could lead to biased model performance, favoring the majority classes, and makes it clear that F1 score is a more appropriate metric than accuracy for evaluating model performance on this dataset. Another issue present is the mislabeling of some samples. After analysing the dataset, we found samples labeled with the wrong emotions. For example, the sentence ``I didn't feel humiliated'' was labeled as \textit{sadness}. The sentence explicitely denies the negative emotion, yet it was labeled with it, presumably due to the presence of the word ``humiliated''. This kind of mislabeling will inevitably cap the performance of any model trained on this dataset, as it introduces noise and confusion during the learning process. Another possible cause is the fact that dataset relies on hastags to label the samples, and their use in sarcastic or ironic contexts is a major source of error.
\noindent


\section{Approach of Supervised Classification}

This approach follows a supervised learning paradigm in which pretrained Transformer models are used as fixed feature extractors, and a lightweight neural classifier is trained on top of the resulting sentence embeddings.
\subsection{Embedding Generation} \label{sec:embedding}

\noindent
\\
The sentence-transformer models are used to encode each sentence into a dense, fixed-length embedding vector. The embedding process is performed in batches to improve efficiency and introduces beneficial stochasticity into the optimization process. The Transformer remains frozen during this stage, meaning that no gradient updates are applied to its parameters.
These embeddings serve as high-level semantic features that capture syntactic and contextual information from the input text. The corresponding emotion labels are retained as integer class indices, resulting in pairs of the form \textit{(embedding, label)}, which are later wrapped into \texttt{DataLoader} objects for mini-batch training and evaluation. On top of the generated embeddings, we trained and evaluated two light classification heads, a Multi-Layer Perceptron and Logistic Regression. 

\vspace{-1em}
\subsection{Classifier Architecture (ANN)}

\noindent
Its architecture is intentionally lightweight, as the main representational power is delegated to the pretrained Transformer embeddings. The architecture consists of:
\begin{itemize}
    \item \textbf{Input Layer:} Matches the dimension of the embeddings.
    \item \textbf{Hidden Layer:} A fully connected layer with 256 neurons, followed by a Rectified Linear Unit (ReLU) activation function, introducing non-linearity into the model.
    \item \textbf{Regularization:} A Dropout layer, implemented in the training, with a probability of $p=0.1$ to prevent overfitting.
    \item \textbf{Output Layer:} A fully connected layer with six neurons, one for each emotion class, producing unnormalized logits.
\end{itemize}

\noindent
Class predictions are obtained by applying the \texttt{argmax} operation over the output logits, selecting the emotion class with the highest predicted score.

\subsection{Classifier Architecture (Logistic Regression)}

\noindent
As an alternative to the neural classifier, a multinomial Logistic Regression (\texttt{LogReg}) model is employed as a lightweight linear classification head on top of the frozen Transformer embeddings. This approach allows us to assess how well linearly separable the embedding space is for the emotion classification task. The Logistic Regression classifier is implemented using the \texttt{scikit-learn} library and trained directly on the precomputed sentence embeddings, without any additional feature transformation or hidden layers. Its architecture can be summarized as follows:
\begin{itemize}
    \item \textbf{Input Layer:} A fixed-size embedding vector whose dimensionality matches the output of the pretrained Transformer model.
    \item \textbf{Linear Decision Function:} A single linear transformation that maps the input embeddings to six output scores, one for each emotion class.
    \item \textbf{Multinomial Softmax:} The class probabilities are obtained via the multinomial logistic \texttt{softmax} formulation.
\end{itemize}

\vspace{-2em}
\section{Approach of Zero-Shot Classification}

\vspace{-1em}
The zero-shot classification approach leverages pre-trained Natural Language Inference (NLI) models to classify text without specific task-related training data. NLI models are initially trained to determine the relationship between a premise and a hypothesis. For this purpose, the template \textbf{\texttt{"This text expresses {}."}} is used (\texttt{NLI.py}).


\vspace{-1em}
\subsection{Architecture}

\begin{enumerate}
    \item \textbf{Batch Inference:} The test texts are processed in batches for computational efficiency.
    \item \textbf{Prediction:} For each text, the model compares the text (premise) against a hypothesis for every candidate label.
    \item \textbf{Label Selection:} The label whose hypothesis yields the highest entailment score is selected as the predicted class for the input text.
\end{enumerate}

\vspace{-1.6em}
\section{Experimental Setup}
\subsection{ANN}

The classifier is trained using the \texttt{Cross-Entropy} loss function, which is standard for multi-class classification tasks. 
To prevent overfitting and ensure robust model selection, \textbf{early stopping} is employed based on the macro-averaged F1 score computed on the validation set. Training is halted if no improvement in validation F1 is observed for a fixed number of consecutive epochs, and the best-performing model state is restored before final evaluation. The ANN utilized in supervised training was configured with the hyperparameters listed in Table~\ref{tab:supervised_hyperparams}. The \texttt{all-MiniLM-L6-v2}, \texttt{paraphrase-mpnet-base-v2} and \texttt{all-mpnet-base-v2} models from the \texttt{sentence-transformers} library were employed to generate sentence embeddings. Their configurations, including embedding dimensions and batch sizes, are summarized in Table ~\ref{tab:supervised_models}. Experiments were conducted using five different seeds to account for variability in training outcomes.

\vspace{-2em}

\begin{table}[H]
\centering
\caption{Hyperparameters for supervised training}
\label{tab:supervised_hyperparams}
\begin{tabular}{lc}
\hline
\textbf{Hyperparameter} & \textbf{Value} \\
\hline
Batch Size & 64 \\
Learning Rate & $1 \times 10^{-3}$ \\
Epochs & 25 with Early Stopping \\
Loss Function & Cross-Entropy Loss \\
Optimizer & Adam \\
Seeds & [2025, 2029] \\
\hline
\end{tabular}
\end{table}

\vspace{-2em}

\begin{table}[H]
\centering
\caption{Supervised Models and their configurations.}
\label{tab:supervised_models}
\begin{tabular}{lc}
\hline
\textbf{Model} & \textbf{Embedding Dimension} \\
\hline
all-MiniLM-L6-v2 & 384 \\
paraphrase-mpnet-base-v2 & 768 \\
all-mpnet-base-v2 & 768 \\
\hline
\end{tabular}
\end{table} 

\vspace{-2em}
\noindent
The selected model is evaluated on the held-out test set using accuracy, macro-averaged F1 score, and confusion matrices. 


\section{Logistic Regression}


The classifier is trained using the \texttt{lbfgs} optimizer with a maximum of 200 iterations and the \texttt{multinomial} setting, which jointly optimizes all class decision boundaries. Unlike the ANN-based classifier, the Logistic Regression head does not require iterative epoch-based training or early stopping. As a result, the model is trained once per embedding configuration using the full training set. Although experiments are repeated across multiple random seeds for consistency with the ANN evaluation protocol, reported metrics for this classifier exhibit zero standard deviation across runs. This behavior is expected, as Logistic Regression is a deterministic linear model and when trained on identical embeddings and labels, it yields the same learned parameters and predictions regardless of the random seed.
\\
After training, the Logistic Regression classifier is evaluated on the test set using accuracy, macro-averaged F1 score, and confusion matrices.


\section{NLI}

As described in Section ~\ref{sec:embedding}, the zero-shot classification approach utilizes pre-trained NLI models to perform emotion classification without task-specific training data. The models evaluated in this approach are summarized in Table~\ref{tab:nli_models}. All models were sourced from the HuggingFace Hub and are tested with a batch size of 32 on a GPU device to optimize inference speed.

\begin{table}[H]
\centering
\caption{NLI Models used for Zero-Shot Classification}
\label{tab:nli_models}
\begin{tabular}{lc}
\hline
\textbf{Model Name} & \textbf{Size (parameters)} \\
\hline
MoritzLaurer/xtremedistil-l6-h256-zeroshot-v1.1-all-33 & $\approx 13$M \\
joeddav/xlm-roberta-large-xnli & $\approx 600$M \\
MoritzLaurer/bge-m3-zeroshot-v2.0 & $\approx 600$M \\
MoritzLaurer/deberta-v3-xsmall-zeroshot-v1.1-all-33 & $\approx 71$M \\
MoritzLaurer/ModernBERT-large-zeroshot-v2.0 & $\approx 400$M \\
facebook/bart-large-mnli & $\approx 400$M \\
\hline
\end{tabular}
\end{table}
\noindent
Unlike the supervised pipeline, the zero-shot approach is applied directly to the test set without any parameter updates. Performance is evaluated using accuracy, macro-averaged F1 score, and confusion matrices, enabling a fair comparison with the supervised methods and providing insight into overall performance as well as robustness to class imbalance.

\section{Results and Discussion}

\subsection{Supervised Models}
The following Table~\ref{tab:model_comparison} summarizes the performance across the different embedding models and classifier heads used in the supervised approach. Results are reported as mean accuracy and mean macro F1 score over the five different seeds, along with the average number of epochs taken for training before early stopping.

\begin{table}[H]
    \centering
    \begin{tabular}{llccc}
        \hline
        \textbf{Model} & \textbf{Head} & \textbf{Accuracy} & \textbf{Macro F1} & \textbf{Avg. Epochs} \\ \hline
        all-MiniLM-L6-v2 & ANN & $0.7315 \pm 0.0061$ & $0.6556 \pm 0.0057$ & 20.0 \\ 
        all-MiniLM-L6-v2 & LogReg & $0.6870 \pm 0.0000$ & $0.5928 \pm 0.0000$ & N/A \\ 
        paraphrase-mpnet-base-v2 & ANN & $0.7089 \pm 0.0037$ & $0.6249 \pm 0.0091$ & 12.4 \\ 
        paraphrase-mpnet-base-v2 & LogReg & $0.6950 \pm 0.0000$ & $0.6083 \pm 0.0000$ & N/A \\ 
        all-mpnet-base-v2 & ANN & $0.7014 \pm 0.0019$ & $0.6177 \pm 0.0059$ & 13.0 \\ 
        all-mpnet-base-v2 & LogReg & $0.6760 \pm 0.0000$ & $0.5629 \pm 0.0000$ & N/A \\ 
        \hline
    \end{tabular}
    \caption{Performance Comparison of Embedding Models and Classifiers}
    \label{tab:model_comparison}
\end{table}

\noindent
The ANN classifier consistently outperforms the Logistic Regression head across all embedding models. Even though this gap is smaller in \texttt{paraphrase-mpnet-base-v2} model, it made a noticeable difference in the other two. This indicates that the non-linear decision boundaries learned by the ANN provide a significant advantage over the linear separability assumed by Logistic Regression, and suggests that the embedding spaces are not linearly separable for the emotion classification task. It is also noticeable that apart from the \texttt{all-MiniLM-L6-v2} model which on average used all 25 epochs, early stopping occurred relatively quickly on the rest, within the first 15 epochs. This suggests that is might've been beneficial to train the first model for longer, as the scores were still rising

\subsubsection{Model Comparison} Among the embedding models evaluated, the highest performance scores were achived by \texttt{all-MiniLM-L6-v2} in both accuracy and macro F1 score, regardless of the classifier head used. However, the performance differences between the embedding models are not substantial, indicating that all three models provide reasonably effective representations for the emotion classification task. The size of the models did also not show much effect on the performance, as the smallest model (with dimension 384) actually performed the best in comparison to the larger ones (with dimension 768). This could be due to the fact that the smaller model is less prone to overfitting.

\vspace{-1em}
\subsection{NLI Models}
\vspace{-1em}
The following table (Table ~\ref{tab:zero_shot_results}) summarizes the performance of the different NLI models used in the zero-shot classification approach. Results are reported in terms of accuracy and macro F1 score on the test set.

\begin{table}[H]
\centering
\caption{Zero-Shot Classification Results}
\label{tab:zero_shot_results}
\begin{tabular}{lcc}
\hline
\textbf{Model Name} & \textbf{ACC} & \textbf{Macro F1} \\
\hline
MoritzLaurer/xtremedistil-l6-h256-zeroshot-v1.1-all-33 & 0.6120 & 0.5148 \\
joeddav/xlm-roberta-large-xnli & 0.3505 & 0.3222 \\
MoritzLaurer/bge-m3-zeroshot-v2.0 & 0.6960 & 0.6232 \\
MoritzLaurer/deberta-v3-xsmall-zeroshot-v1.1-all-33 & 0.7045 & 0.6306 \\
MoritzLaurer/ModernBERT-large-zeroshot-v2.0 & 0.7090 & 0.6304 \\
facebook/bart-large-mnli & 0.4660 & 0.4333 \\
\hline
\end{tabular}
\end{table}


\subsubsection{Model Comparison:} The results indicate significant variability in performance across the different NLI models. The model that achieved the highest macro F1 score of 0.6304 was \texttt{MoritzLaurer/ModernBERT-large-zeroshot-v2.0}, closely followed by \texttt{MoritzLaurer/deberta-v3-xsmall-zeroshot-v1.1-all-33} with a macro F1 of 0.6306. The worst performing model achieved a macro F1 score of only 0.3222 and was \texttt{joeddav/xlm-roberta-large-xnli}.
\noindent
\\
Interestingly, model size did not correlate strongly with model performance. For example, \texttt{MoritzLaurer/deberta-v3-xsmall-zeroshot-v1.1-all-33}, which is relatively small, with $\approx 71$M parameters, outperformed the largest models such as \texttt{joeddav/xlm-roberta-large-xnli} with $\approx 600$M parameters. Models with similar sizes also exhibited varying performance, like \texttt{facebook/bart-large-mnli} and \texttt{MoritzLaurer/ModernBERT-large-zeroshot-v2.0}, both around $\approx 400$M parameters.
\noindent
\\
This discrepancy is due to a different reason: what each model was taught during pretraining. Taking \texttt{joeddav/xlm-roberta-large-xnli} and \texttt{deberta} as examples. On the one hand, \texttt{xlm-roberta} was trained in a multilingual setting, which makes it less specialized in English tasks, and on standard NLI datasets, focusing on logic reasoning. On the other hand, \texttt{deberta} was trained in a monolingual setting, specifically for zero-shot classification tasks, and its training included emotion detection, sentiment, and topic classification, which is far more relevant to our task. This proved to be a major factor when it came to performance and that, given a different task, the results could be completely different.
\noindent
\\
This difference could also be attributed to the architecture of each model, as some may already have an outdated architecture that doesn't perform as well as more recent ones. For example, \texttt{facebook/bart-large-mnli} is based on the BART architecture, which, while powerful, has been surpassed by newer architectures like DeBERTa and ModernBERT in various NLP tasks.

\subsection{Supervised vs Zero-Shot Classification}
After analyzing the results from both the supervised and zero-shot classification approaches, several key observations can be made regarding the best performing models and their comparative performance.
The supervised ANN classifier, despite relying on a comparatively smaller embedding model (\texttt{all-MiniLM-L6-v2}), demonstrates superior overall performance when compared to the zero-shot NLI-based approach.In particular, the supervised method achieves higher scores across the main evaluation metrics:
\begin{itemize}
    \item \textbf{Accuracy:} Supervised ANN (mean = 0.7315) outperforms NLI (0.7045).
    \item \textbf{Macro F1:} Supervised ANN (mean = 0.6556) exceeds NLI (0.6306).
\end{itemize}

\subsubsection{Performance Analysis}
A class-level analysis of the recall (true positive rate) reveals where each model excels, based on the row-normalized confusion matrices. Recall is derived from the diagonal elements of the row-normalized matrices.

\begin{table}[H]
\centering
\caption{Recall Comparison on Test Set (Supervised ANN vs. NLI Zero-Shot)}
\label{tab:recall_comparison}
\begin{tabular}{lccc}
\hline
\textbf{Emotion} & \textbf{Supervised ANN} & \textbf{NLI Zero-Shot} & \textbf{Difference} \\
\textbf{Class Index} & \textbf{Recall} ($\pm \sigma$) & \textbf{Recall $\pm \sigma = 0$} & \textbf{(ANN - NLI)} \\
\hline
Joy (0) & $0.797 \pm 0.010$ & $0.764$ & $+0.033$ \\
Sadness (1) & $0.801 \pm 0.007$ & $0.722$ & $\mathbf{+0.079}$ \\
Anger (2) & $0.484 \pm 0.016$ & $0.722$ & $\mathbf{-0.238}$ \\
Fear (3) & $0.678 \pm 0.024$ & $0.709$ & $-0.031$ \\
Love (4) & $0.682 \pm 0.017$ & $0.683$ & $-0.001$ \\
Surprise (5) & $0.403 \pm 0.015$ & $0.500$ & $\mathbf{-0.097}$ \\
\hline
\end{tabular}
\end{table}

\noindent
The data in Table 7 immediately highlights the discrepancy in performance for two of the most critical classes:
\begin{itemize}
    \item The \textbf{Supervised ANN} exhibits the highest recall for the \texttt{Joy} and \texttt{Sadness} classes, resulting in a considerable advantage of $\mathbf{+0.079}$ for \texttt{Sadness} over the NLI model. This suggests that the supervised training effectively utilizes the embeddings to distinguish these two largest classes.
    \item The \textbf{NLI Zero-Shot} model exhibits a clear and substantial advantage in classifying the \texttt{Anger} class, with a difference of $\mathbf{-0.238}$ in recall compared to the Supervised ANN. Furthermore, the NLI model performs significantly better on the least represented \texttt{Surprise} class ($\mathbf{-0.097}$), indicating a more robust handling of certain minority classes, likely due to the generalized semantic knowledge of the base \texttt{NLI} model.
\end{itemize}
\vspace{-0.3\baselineskip} % Adjust value as needed to remove unwanted space
\noindent
Overall, the Supervised ANN achieved a higher overall Macro F1 score, primarily driven by its superior recall on the largest classes, \texttt{Joy} and \texttt{Sadness}. However, the NLI model's strong performance on \texttt{Anger} and \texttt{Surprise} demonstrates the competitive nature of zero-shot transfer learning, especially where the supervised head struggles to learn complex boundaries.
\vspace{-1.5\baselineskip} % Adjust value as needed to remove unwanted space

\begin{figure}[H]
\centering
\includegraphics[width=0.48\textwidth]{cov_percentage_supervised.png}\hfill
\includegraphics[width=0.48\textwidth]{cov_percentage_NLI.png}
\caption{Covariance percentage heatmaps: Supervised (left) vs. NLI (right).}
\end{figure}

\vspace{-1.5\baselineskip}

\begin{figure}[H]
\centering
\includegraphics[width=0.48\textwidth]{cov_values_supervised.png}\hfill
\includegraphics[width=0.48\textwidth]{cov_values_NLI.png}
\caption{Covariance values heatmaps: Supervised (left) vs. NLI (right).}
\end{figure}

\noindent
\\
The supervised model's most significant error pattern is the misclassification of \texttt{Anger} (2), which is directly responsible for the low \texttt{Anger} recall.
\begin{itemize}
    \item \textbf{Misclassification of Anger (2):} The single largest source of error in the entire supervised analysis is the misclassification of true \texttt{Anger} samples. Approximately $35.5\%$ of texts expressing \texttt{Anger} were wrongly predicted as \texttt{Sadness}. A further $8.1\%$ were misclassified as \texttt{Joy}. This suggests that the ANN classifier, trained on \texttt{all-MiniLM-L6-v2} embeddings, maps \texttt{Anger} texts into a semantic region that heavily overlaps with \texttt{Sadness}.
    \item \textbf{Misclassification of Surprise (5):} The model struggles significantly with the smallest class, \texttt{Surprise}, misclassifying $26.4\%$ of its samples as \texttt{Sadness} and $18.2\%$ as \texttt{Love}.
\end{itemize}

\noindent
\\
The NLI model, while robust on \texttt{Anger} and \texttt{Surprise}, shows greater difficulty distinguishing between other negative emotions.
\begin{itemize}
    \item \textbf{Confusion of Sadness and Love with Joy:} The model misclassifies $9.2\%$ of true \texttt{Sadness} samples and $13.8\%$ of true \texttt{Love} samples as \texttt{Joy}. This indicates a tendency of the NLI model to conflate positive and negative emotions, particularly when the emotional content is subtle or ambiguous.
    \item \textbf{Confusion of Surprise with Sadness:} Similar to the supervised method, the NLI model's primary confusion for \texttt{Surprise} is \texttt{Sadness}, with $19.7\%$ of \texttt{Surprise} samples being misclassified.
\end{itemize}

\end{document}
